#!/usr/bin/env node

// This is a stub that will be replaced by the TypeScript build
// For development, use: tsx bin/clawpulse.ts
console.log("Please build the CLI first: cd cli && npm run build");
console.log("Or run with tsx: cd cli && npm run dev");
